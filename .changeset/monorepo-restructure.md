---
"@plyflow/core": minor
"@plyflow/tui": minor
"@plyflow/cli": minor
"plyflow": minor
---

Restructure into @plyflow/* monorepo packages (core, tui, cli) with a `plyflow` meta-package that preserves the existing install path and library import.
